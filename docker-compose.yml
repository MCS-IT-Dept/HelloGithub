services:
  web:
    build: .
    container_name: hellogithub-web
    ports:
      - "${PORT:-5000}:5000"
    environment:
      FLASK_APP: app.py
      FLASK_ENV: ${FLASK_ENV:-production}
    restart: unless-stopped
